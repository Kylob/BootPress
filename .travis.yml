language: php

php:
  - '5.4'
  - '5.5'
  - '5.6'
  - '7.0'
  - hhvm
  - nightly

before_script:
  - if [[ "$TRAVIS_PHP_VERSION" != '5.4' ]]; then composer install -n ; fi

script:
  - if [[ "$TRAVIS_PHP_VERSION" != '5.4' ]]; then phpunit ; fi
