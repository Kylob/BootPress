language: php

php:
  - '5.4'
  - '5.5'
  - '5.6'
  - '7.0'
  - hhvm
  - nightly

before_script:
  - composer install --dev
  - if [[ "$TRAVIS_PHP_VERSION" < '7.0' ]]; then composer require paragonie/random_compat:1.* -n ; fi
  - if [[ "$TRAVIS_PHP_VERSION" < '5.5' ]]; then composer require ircmaxell/password-compat:1.* -n ; fi

script:
  - phpunit --debug
